---
layout: stp
---
[
  {% for activity in site.data.activities %}
  {
    "titel"        : "{{ activity.title | escape }}",
    "sprekers"     : [
    {% for speaker in activity.speakers %}  "{{speaker}}"{% if forloop.last %}{% else %},{% endif %}
    {% endfor %}],
    "beschrijving" : "{{ activity.description | escape }}",
    "type"         : "{{ activity.type }}"
  },
  {% endfor %}
]
